# v0.1 final release review

Final release-gate review of `phlo-transform` as v0.1.0. Scope: verify the
baseline, run the full validation suite live, check packaging/install/
upgrade paths, and record a verdict. Not an architecture review.

## Baseline

| Item | Value |
|---|---|
| Commit reviewed | `5fcb695698a7b344dccde8a2a6ddb12623299502` (`main`, post-PR #18) |
| Rust toolchain | `rustc 1.93.0 (254b59607)`, pinned via `.mise.toml` (`rustfmt`, `clippy`) |
| Crate versions | all 10 workspace crates at `0.1.0` (workspace-inherited) |
| `rust-version` | `1.93` |
| Compiler semantics | `COMPILER_SEMANTICS_VERSION = 3` (`phlo-transform-core/src/version.rs`) |
| Artifact schema | `SCHEMA_VERSION = 3` (`phlo-transform-engine/src/artifacts.rs`) |
| Daemon API | `/v1/...` — reads, `POST /v1/operations`, cancel, reload |
| Adapters | `duckdb` (embedded, bundled) and `trino` (HTTP) |
| State stores | SQLite (default `.phlo/transform/state.db`), Postgres (`--state postgres://…`, `NoTls`) |

Feature claims in `docs/v0.1-readiness.md` verified against code: executable
`cached` adoption (`PlanAction::Cached` → verified adopt-or-build path in
`run.rs`), catalog-free target version input (`schema.table|materialization`),
environment-qualified source compilation, composite-key assertions,
`ref delete` catalog cleanup, Trino `load_csv`, retryable Nessie/identity-read
classification, evidence-id-bound promotion records, and the
`skip`/`cached`/`build` semantics all exist as described.

## Validation

Run on this checkout (macOS arm64, OrbStack Docker):

| Suite | Command | Result |
|---|---|---|
| Format | `cargo fmt --all -- --check` | clean |
| Lint | `cargo clippy --workspace --all-targets -- -D warnings` | clean |
| Unit/integration | `cargo test --workspace` | **582 passed, 0 failed**, 10 ignored |
| Postgres shared-state | `PHLO_TEST_POSTGRES_URL=… cargo test -p phlo-transform-engine` | 5/5 vs real Postgres 16 |
| Trino e2e | `--test trino_e2e -- --ignored` | 3/3 (run+test, keyed diff, partition/tolerance/schema/sampling diff) |
| Nessie WAP | `--test nessie_wap_e2e -- --ignored` | 2/2 (WAP promotion; candidate adopts inherited materialisations, zero model SQL) |
| Daemon env isolation | `--test nessie_e2e -- --ignored` (daemon crate) | 1/1 (candidate run cannot touch base tables) |
| Incremental MERGE | `--test trino_incremental_e2e -- --ignored` | 1/1 (Trino MERGE on Iceberg) |
| CLI golden path | `--test golden_path -- --ignored` | 1/1 (init→promote on live Trino+Nessie) |
| Dogfood (DuckDB) | `scripts/dogfood.sh` | 16/16 |
| Dogfood (live) | `PHLO_TRINO_ENDPOINT`+`PHLO_NESSIE_ENDPOINT`+`PHLO_NESSIE_CATALOG_URI` | **24/24** — incl. `run --ref` asserted `cached` for every model via JSON, `diff`, `promote --check`, `ref delete` cleanup |

The 10 ignored tests are the eight Docker e2e above plus two scaling
benchmarks run explicitly — `plan_scaling_e2e` on real Trino and
`plan_scaling_adapter_metadata` against a counting adapter. All Docker e2e
were run here and pass.
Dogfood output was clean: no warnings, no retries, no suspicious skips; the
warm run reported all-skip as designed.

## Clean install

```bash
CARGO_INSTALL_ROOT=/tmp/… cargo install --path crates/phlo-transform-cli --locked
```

Installs `phlo-transform` 0.1.0. On a fresh directory:
`init` → `check` → `run --adapter duckdb` → `plan` (all-skip second time) →
`doctor` → `explain` all pass. No repository-relative assumptions; the bundled
DuckDB adapter needs no services. The documented Trino/Nessie path is covered
by the live dogfood leg and `golden_path` e2e.

`phlo-transform --version` → `phlo-transform 0.1.0`. The daemon `/v1/status`
payload now also reports `version` alongside `compiler_semantics_version`
(added this pass, asserted in `tests/api.rs`).

## Packaging

All manifests carry `name`, `version` (0.1.0), `edition 2021`, `license =
Apache-2.0`, `repository`, `rust-version`, `description`. Internal
dependencies are `path + version = "0.1.0"` — the publishable form.

`cargo package --allow-dirty` results:

| Crate | Result |
|---|---|
| `phlo-transform-sql`, `phlo-transform-nessie` | package **and** verify-build cleanly |
| `phlo-transform-core`, `-dbt`, `-engine`, `-openlineage`, `-trino`, `-daemon`, `-duckdb`, `-cli` | **package file lists are clean**, but full `cargo package` cannot resolve until their internal deps exist on crates.io — expected pre-publish |

Package contents inspected: `src/`, `tests/` (+ insta snapshots), manifests
only — no fixtures, no generated state, no Docker/testcontainers assets, no
oversized files.

**Publish order** if crates.io release is ever wanted:
`sql` → `core`, `nessie`, `openlineage`, `dbt` → `engine` →
`duckdb`, `trino`, `daemon` → `cli`. No crate is marked `publish = false`;
the v0.1 distribution channel is the GitHub-release binaries built by
`release.yml`, so crates.io staging does not gate this release.

## Upgrade compatibility

Verified by hand-building realistic old state and opening it with the
installed binary:

- **SQLite** — a pre-evidence, pre-`effective_key`, pre-`output_identity`,
  pre-v3 database (no `evidence`/`promotions` tables, short column sets,
  `compiler_version='2'` record): opens cleanly; all tables created and
  columns added **additively**; the legacy row reads via `state model`
  (`Adapter: <unrecorded>`); `plan` then reports `compiler semantics changed`
  and rebuilds once — the documented one-time rebuild wave.
- **Postgres** — a faithful old schema (BIGINT counters, no `evidence` table,
  no `adapter`/`output_identity`/`effective_key_json`/`seq`/`plan_json`
  columns): `ensure_schema` adds everything non-destructively;
  `state runs`/`state model` read pre-existing rows correctly.
- **Artifacts** — `lineage_diff.json`, `environment*.json` and promotion
  `record_json` carry `#[serde(default)]` on every field added since they
  were first written (`base_kind`, `candidate`, `environment`, `created_at`,
  `catalog_status`, `catalog_owned_by_phlo`, `evidence`); old payloads
  deserialize. `manifest`/`plan`/`run`/`diff`/`branch_diff` artifacts are
  write-only — the tool never reads them back.
- **Intentional incompatibility** — none found beyond the designed ones:
  pre-v3 records hash-miss once and rebuild; records without
  `adapter`/`output_identity` can never authorise cache reuse (fail closed).

Edge case noted, not a blocker: pointing `--state postgres://` at a
*foreign* table named `runs` with incompatible column types kills the state
worker thread; the CLI exits `error: state store error: postgres worker
stopped`. Loud and contained — phlo-written schemas always used BIGINT
counters, so this only triggers on databases phlo never created.

## Security / hygiene

- No credentials, tokens, `.env` files or private endpoints tracked; CI uses
  a throwaway `phlo:phlo` service password only.
- Postgres URLs have credentials stripped before they can reach error text
  (`state_location_display`), covered by `unreachable_postgres_state_url_errors`.
- VHS doc-generation tapes hardcoded a developer's home path — **fixed** to a
  generic `PATH` + comment.
- `.gitignore` covers `.phlo/`, `target/`, corpus data (285M local corpus is
  untracked; only `corpus/packages.lock` is committed, intentionally).
- All 36 fixture dirs are referenced by tests; no stale scripts; README links
  all resolve; CI test-target names all exist.
- **`LICENSE` was missing** despite `license = "Apache-2.0"` on every crate
  and `release.yml` copying `LICENSE` into release tarballs — **added** the
  Apache-2.0 text.
- Stale `cached` "classification only" claims corrected: `docs/roadmap/README.md`
  Phase-3 row updated; `docs/convergence-review.md` got a point-in-time note
  (it predates PR #17/#18; left as a historical record otherwise).

## Documentation audit

README covers: what it is, install, DuckDB quickstart, Trino env vars,
selectors, dbt translation, and honest per-phase status with links.
`docs/engine.md` documents `skip`/`cached`/`build` and cache adoption;
`docs/wap.md` environments/refs/promotion; `docs/state.md` versions and
migrations; `docs/daemon.md` the machine API; `docs/dbt-migration*.md` the
translator and its limits; `docs/validation.md` + `docs/release-validation.md`
record the hands-on passes. A new user can reach every required concept from
the README's doc links.

Release notes drafted at [`docs/v0.1-release-notes.md`](v0.1-release-notes.md).

## CI / release workflow

`ci.yml` is the release gate: fmt + clippy + `cargo test --workspace` + the
Postgres service tests + all five Docker e2e targets. `release.yml` fires only
on `v*` tags: builds the CLI for linux-x86_64, macOS arm64 and macOS x86_64,
packages tarballs, creates a GitHub release. Nothing auto-publishes crates or
tags anything. No changes needed.

## Known limitations

- Warm-plan cost is O(models × snapshot read): ~28s at 5,000 models on a
  small Nessie/Trino pair (`--since` stays sub-second).
- Postgres state has no TLS — trusted networks only.
- Nessie Iceberg catalogs cannot store views — views must materialise as
  tables.
- dbt dynamic Jinja is deliberately limited: macros/snapshots/`run_query` are
  `REVIEW`/`UNSUPPORTED`, never executed.
- No cross-adapter/cross-engine cache reuse by design.
- `plan --ref` on an unprovisioned candidate previews an empty target (a
  printed note explains `run --ref` provisions and may adopt).
- Daemon reload is a full recompile; responses terser than the CLI.

## Release blockers

None known. (The missing `LICENSE` and the stale `cached` claims were the
only real findings; both are fixed in this pass.)

## Final verdict

**Ready for v0.1.0**

## Post-v0.1 priorities

1. Batched/alternative output-identity reads — the ~28s warm plan at 5k is
   the largest recurring cost; every candidate run pays it.
2. Postgres TLS support — required before shared state over untrusted
   networks.
3. Provisioned-candidate `plan` parity — show expected adoption without a
   preceding `run --ref`.
4. `state`/`state evidence` ergonomics — make the promotion → evidence id →
   payload join legible in human output.
5. Transient-resilience budget review at 10k+ models — confirm the
   150/400ms retry delays and `buffered(16)` window still suffice.
