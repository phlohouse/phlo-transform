name: jaffle
profile: jaffle
version: "1.0"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

vars:
  region: 'eu'

models:
  jaffle:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +tags: ['mart']
