version: 2

sources:
  - name: raw
    schema: landing
    tables:
      - name: events

models:
  - name: stg_events
    description: Staged events
    columns:
      - name: id
        tests: [not_null]
